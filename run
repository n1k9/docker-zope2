#!/bin/bash

INSTANCE_HOME="$ZOPE_HOME/instance"
Z=/src/Zope/bin

if [ ! -d "$INSTANCE_HOME/etc" ]; then
  sudo -u zope -g zope $Z/mkzopeinstance -u zope:2epoz -d $INSTANCE_HOME
  sudo -u zope -g zope cp /zope.conf $INSTANCE_HOME/etc/
fi

/zope/instance/bin/zopectl fg
